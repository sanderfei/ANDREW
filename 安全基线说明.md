# 安全基线说明

## 已完成

- 现有 `1_prompt` 和 `2_langchain` 课件不再把 provider / LangSmith 凭据写进源码；它们通过各目录的 `_runtime.py` 复用根目录 `learning_runtime.py`，仍可直接用 `python 路径/课件.py` 运行。
- 根目录新增 `.env.example`。复制成 `.env` 后填写自己的变量；该文件已被 `.gitignore` 排除。
- 新的 `4_rag_knowledge_base_service` 默认 `RAG_MODE=offline`，不需要 key；live 模式才校验 `OPENAI_API_KEY`。

## 使用方式

```bash
cp .env.example .env
# 编辑 .env
set -a && source .env && set +a
.venv/bin/python 2_langchain/L3_Function_Calling_In_Langchain.py
```

也可以只在当前 shell 导出所需变量。不要把 key 粘贴回 Python 文件、README 或命令历史。

## 仍需你在 Provider 控制台完成的动作

历史提交可能已包含旧凭据文本。请尽快在对应 provider 和 LangSmith 控制台撤销或轮换这些凭据；本次没有重写 Git 历史，避免在未确认的情况下改变仓库历史。轮换后只把新值写入本机未提交的 `.env`。
