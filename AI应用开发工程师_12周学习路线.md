# AI 应用开发工程师：12 周学习路线

## 目标与推进原则

目标不是训练基础模型，而是可以独立交付、评估、部署并维护一个有边界的 AI 应用：先完成稳定的两步 RAG，再把检索能力包装为 Agent 工具，最后补齐评估、可观测性与上线工程。

主线固定为：Python → 原生 LLM API → LangChain → RAG → FastAPI / Chroma → LangGraph → LangSmith → Docker / CI。

现有课件维持各自独立运行；共有的 RAG 逻辑已经集中在 `3_rag_from_scratch/_common.py`。新增综合项目不反向耦合旧课件。

## 当前学习顺序与周产出

| 周期 | 目录 / 阶段 | 学习重点 | 可验收产出 |
| --- | --- | --- | --- |
| 第 0 周 | 工程基线 | `.env`、依赖边界、运行产物忽略、直接运行约定 | `.env.example`；所有凭据由环境变量提供；每个公开入口有运行命令 |
| 第 1 周 | `1_prompt` | 任务拆解、边界条件、JSON、few-shot、多轮上下文 | 同一业务的 10 组正常 / 异常 Prompt 用例与复盘 |
| 第 2–3 周 | `2_langchain` | 原生 tool calling、LCEL、Agent 循环、结构化输出、State/Context/Store、Middleware、MCP、流式 | 能手写工具数据流，并解释运行时依赖、长期数据和调用控制边界 |
| 第 4 周 | `3_rag_from_scratch` | indexing、`embed_documents`、`embed_query`、chunk、retriever、引用、两步 RAG | 记录至少三组 chunk 参数的检索差异 |
| 第 5–7 周 | `4_rag_knowledge_base_service` | Markdown/PDF 摄取、Chroma 持久化、增量索引、拒答、API | 可离线运行的本地知识库服务与 15+ 条评测 |
| 第 8–9 周 | `5_langgraph_agentic_rag` | 状态、路由、重试、HITL、持久化、并行/`Send`、子图、异步 Streaming、Functional API | 一个受控 Agentic RAG 流程，并掌握复杂 Graph 的单 Agent 运行基础 |
| 第 10 周 | 目录 5 合同评测 + LangSmith | 检索命中、引用有效、拒答正确、忠实度；离线回归与 trace | 黄金问题集、评测报告、LangSmith 实验记录 |
| 第 11 周 | 上线工程化 | 配置分层、日志、请求 ID、超时、限流、Docker、CI | 可复现启动与持续集成检查 |
| 第 12 周 | 作品集与面试 | 架构、API、评测、部署、演示、限制与迭代 | 可演示项目说明和 3 分钟演示脚本 |

## 进入 Agent Harness 前的基础完成线

学完 `2_langchain` L1～L15 和 `5_langgraph_agentic_rag` Part 1～14 后，LangChain / LangGraph
前置基础视为完成：Tool/Message/Agent、RAG、State/Reducer、持久化与 HITL、Runtime
Context/Store、Middleware、MCP、并行与 `Send`、子图、异步 Streaming 和 Functional API
均已有真实可运行路径。后续可以直接进入 Harness，不必先返回补一轮框架基础。

Harness 阶段再学习多 Agent 协议、长上下文预算、长期记忆策略、文件系统与 Sandbox、
任务队列、权限安全、Tracing/Eval 和生产部署；这些是系统层能力，不是本阶段遗漏的基础 API。
Node cache、`defer`、批量历史更新等低频接口按具体项目需要查用，不单独占一课。

基础补充按下面顺序学习，前五项保持“运行时数据 → 并行 → 子图 → Middleware → 异步”的
依赖关系，再补 Harness 常用协议和另一种工作流写法：

```text
L13 Runtime Context / Store
→ Part 11 Parallel / Send
→ Part 12 Subgraph
→ L14 Middleware
→ Part 13 Async / Streaming
→ L15 MCP
→ Part 14 Functional API
```

## 每周节奏

每周投入 8–10 小时：2 小时读官方资料，4–5 小时手写实现，1–2 小时测试或评估，1 小时复盘。仅当能脱离课件重写、解释数据流、举出失败案例后再进入下一阶段。

## 当前综合项目完成线

`4_rag_knowledge_base_service` 必须返回 `answer`、`answerable`、`citations`、`retrieval` 与 `request_id`；默认离线，不把凭据放入源码；保存稳定 chunk ID、来源和 metadata；支持增量重建；包含 API 冒烟、15 条以上评测、Docker、CI、架构文档与演示脚本。

完成它以后，才把 retriever 封装成 LangGraph 工具。这条顺序刻意先保证可预测性和可评估性，再增加 Agent 的自主性。

## 参考资料

- [LangChain Retrieval / RAG](https://docs.langchain.com/oss/python/langchain/retrieval)
- [LangChain Knowledge Base](https://docs.langchain.com/oss/python/langchain/knowledge-base)
- [OpenAI Responses API migration guide](https://developers.openai.com/api/docs/guides/migrate-to-responses)
